# Core local analysis environment recorded from the successful RTX 5090 run.
# PyTorch is intentionally not pinned here: install the CUDA build appropriate
# for the target workstation before this file, or clone the known-good local
# environment described in docs/LOCAL_REPRODUCTION.md.
numpy==2.4.6
pandas==3.0.3
scipy==1.15.3
scikit-learn==1.6.1
matplotlib==3.11.0
seaborn==0.13.2
xgboost==3.2.0
openpyxl==3.1.5
shap==0.51.0
shapiq==1.4.1
tabpfn==8.2.0
tabpfn-extensions[interpretability]==0.4.1
